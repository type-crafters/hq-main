"use client";

import { JSX } from "react";

export default function WebsiteHeader(): JSX.Element {
    return(
        <header className="flex w-full bg-black p-4 border border-neutral-400">
        
        </header>
    );
}